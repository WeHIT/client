const BASE_URL = 'http://127.0.0.1:7001';

const urlMap = {
  'login': `${BASE_URL}/login`,
  'reg': `${BASE_URL}/reg`,
  'api': `${BASE_URL}/api`,
  'post': `${BASE_URL}/post`,
};

export default urlMap;
